# Usage

## Initialization
1. Copy `.env.example` to `.env`
2. Populate `.env` with your Environment ID and Secret Key
3. `npm install`

## Local Development
`npm run dev`

## Deployment
`npm run deploy`
